Até o momento da aula ao vivo, volte aos exercícios do dia anterior, 4.2, e crie um algoritmo para cada.

***** 	EXERCÍCIOS DIA 4.2   *****

Aprofunde seus conhecimentos
Leia atentamente os enunciados e faça o que se pede! Iremos utilizar esse array para realizar os exercícios do 1 ao 7:

let numbers = [5, 9, 3, 19, 70, 8, 100, 2, 35, 27];

1 - percorra o array imprimindo todos os valores nele contidos com a função console.log()

	1. Adicionar o array numbers;
	2. Criar o loop for que percorre o array;
	3. Adicionar o comando console.log() a cada loop.
	
2 - some todos os valores contidos no array e imprima o resultado;

	1. Adicionar o array numbers;
	2. Criar uma variável soma com o valor 0;
	3. Criar o loop for que percorre o array;
	4. Incrementar a variável soma com o valor correspondente a cada loop;
	5. Imprimir o valor da variável soma.
	
3 - calcule e imprima a média aritmética dos valores contidos no array;
obs.: A média aritmética é o resultado da soma de todos os elementos divido pelo número total de elementos.

	1. Adicionar o array numbers;
	2. Criar uma variável soma com o valor 0;
	3. Criar uma variável mediaAr com o valor 0;
	4. Criar o loop for que percorre o array;
	5. Incrementar a variável soma com o valor correspondente a cada loop;
	6. Atribuir a variavel mediaAr o valor da variável soma dividido pelo valor do tamanho da array numbers;
	7. Imprimir o valor da variável mediaAr.
	
4 - Com o mesmo código do exercício anterior, caso o valor final seja maior que 20, imprima a mensagem: "valor maior que 20". Caso não seja, imprima a mensagem: "valor menor ou igual a 20";

	1. Criar um if com a condição da variável mediaAr ser maior que 20;
	2. Caso a variável atenda a condição;
		1. Imprimir com console.log() "valor maior que 20".
	3. Caso a variável não atenda a condição;
		1. Imprimir com console.log() "valor menor ou igual a 20".
	
5 - Utilizando for , descubra qual o maior valor contido no array e imprima-o;

	1. Adicionar o array numbers;
	2. Criar uma variável maior com valor 0;
	3. Criar um loop for que percorra o array;
	4. Criar um if com a condição do valor correspondente de cada loop ser maior que a variável maior;
	5. Caso a condição seja verdadeiro;
		1. Atribuir o valor correspondente do loop a variável maior.
	6. Caso a condição seja falsa;
		1. Não fazer nada e continuar com o loop.
	7. Imprimir o valor da variável maior;
	
6 - Descubra quantos valores ímpares existem no array e imprima o resultado. Caso não exista nenhum, imprima a mensagem: "nenhum valor ímpar encontrado";

	1. Adicionar o array numbers;
	2. Criar um array impares vazio;
	3. Criar um loop for que percorra o array numbers;
	4. Criar um if com a seguinte condição: o módulo de 2 do valor correspondente de cada loop deve ser diferente de 0;
	5. Caso a condição seja verdadeira;
		1. Adicionar o valor correspondente do loop no array impares com o comando .push().
	6. Caso a condição seja falsa;
		1. Não fazer nada e continuar com o loop.
	7. Criar um if com a seguinte consição: o tamanho do array impares deve ser diferente de 0;
	8. Caso a condição seja verdadeira;
		1. Imprimir o array impares.
	9. Caso a condição seja falsa;
		1. Imprimir "nenhum valor ímpar encontrado".

7 - Utilizando for , descubra qual o menor valor contido no array e imprima-o;

	1. Adicionar o array numbers;
	2. Criar uma variável menor com valor de índice 0 do array numbers;
	3. Criar um loop for que percorra o array;
	4. Criar um if com a condição do valor correspondente de cada loop ser menor que a variável menor;
	5. Caso a condição seja verdadeiro;
		1. Atribuir o valor correspondente do loop a variável menor.
	6. Caso a condição seja falsa;
		1. Não fazer nada e continuar com o loop.
	7. Imprimir o valor da variável menor.

8 - Utilizando for, crie um array que vá de 1 até 25 e imprima o resultado;

	1. Criar um array arr vazio;
	2. Criar um loop for que percorra de 0 a 25;
	3. Adicionar a cada loop o valor de i + 1 no array arr como comando .push();
	4. Imprimir o array arr.

9 - Utilizando o array criado no exercício anterior imprima o resultado da divisão de cada um dos elementos por 2.

	1. Criar um loop for/of com a nova variável divide2;
	2. A cada loop, dividir o valor de arr por 2;
	3. Imprimir a variável divide2;
	
	
Após escrever cada algoritmo, responda novamente as seguintes perguntas:
Eu resolvi o problema? - Sim
Havia outras maneiras de resolver o problema? - Com certeza
A maneira que eu escolhi foi a mais eficiente possível? - Na maior parte dos exercícios não
Seria possível inverter ou retirar algum passo? - Sim
Se eu fosse um computador, conseguiria entender todas as intruções? - Sim
